version: "1"

versioning:
  strategy: calver
  format: "YYYY.MM.PATCH"
  tag_prefix: ""

changelog:
  generator: git-cliff
  output: CHANGELOG.md

release:
  notes:
    generator: git-cliff
  platforms:
    - platform: gitlab
      name: gitlab
