version: "1"

versioning:
  strategy: calver
  format: "YYYY.MM.PATCH"
  tag_prefix: ""

changelog:
  generator: git-cliff
  output: CHANGELOG.md

forges:
  - name: gitlab
    platform: gitlab

release:
  notes:
    generator: git-cliff
  targets:
    - forge: gitlab
